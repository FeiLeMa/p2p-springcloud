spring:
  application:
    name: app-test
  profiles:
    active: dev
  cloud:
    config:
      profile: dev
      name: app-public,app-test
      url: http://localhost:8888
      fail-fast: true
      username: admin
      password: admin
